# git 명령어 정리
- git init: 저장소 초기화, 현재 경로를 git으로 관리하겠다고 선언
- git status: 현재 저장소에 대한 상태를 확인
- git add: git 인덱스에 파일을 추가
  - git add . 으로 현재 경로의 모든 파일을 추가할 수 있다.
- git commit: 변경사항 확정
  - git add 로 인덱스에 추가한 변경사항들을 확정한다.
- git push: git commit으로 확정한 내용들을 원격 저장소에 올린다.
- git checkout: 브랜치를 변경할 때 사용
